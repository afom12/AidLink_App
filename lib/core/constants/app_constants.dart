class AppConstants {
  static const String appName = 'AidLink';
  static const String apiBaseUrl = 'https://api.your-backend.example.com';
  static const Duration connectTimeout = Duration(seconds: 12);
  static const Duration receiveTimeout = Duration(seconds: 20);
  static const Duration urgentRefreshInterval = Duration(seconds: 30);

  static const List<String> supportedLocales = ['en', 'am'];
}




